# Copy this file to ".env" and fill in your real MySQL credentials.
# Command to copy on Windows:  copy .env.example .env
# Command to copy on Mac/Linux: cp .env.example .env

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=cms_moderation

PORT=5000
